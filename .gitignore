# Netlify build output (regenerated on every deploy)
public/

# Local preview artifacts (regenerated by build_preview.py)
preview.html
_preview_fragment.html

# Secrets — never commit
gh-deploy-token.txt

# Python / system cruft
__pycache__/
*.pyc
*.bak
.DS_Store
